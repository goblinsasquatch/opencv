# Setup OpenCV on Raspberry PI OS to serve over Flask App

The following packages must be installed.

```
sudo apt install git libopenjp2-7-dev libhdf5-dev  libatlas-base-dev libjasper-dev python3-pip python3-h5py python3-opencv python3-venv remoteit
```
